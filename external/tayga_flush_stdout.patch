diff -rupN a/tayga.c b/tayga.c
--- a/tayga.c	2025-12-20 20:34:48.000000000 +0100
+++ b/tayga.c	2025-12-20 20:35:22.229819796 +0100
@@ -48,8 +48,10 @@ void slog(int priority, const char *form
 	va_list ap;
 
 	va_start(ap, format);
-	if (use_stdout)
+	if (use_stdout) {
 		vprintf(format, ap);
+		fflush(stdout);
+	}
 	else if (priority != LOG_DEBUG)
 		vsyslog(priority, format, ap);
 	va_end(ap);
